- How to understand this project quickly: Rely on these `.ai/` files. This is an experimental UI lab/sandbox, not a standard blog. It uses a 3-layer visual paradigm (Background, Overlay, Content Panel).
- How to avoid unnecessary edits: Do not refactor legacy code (e.g. duplicate PostCards) or large files unless specifically requested. Do not touch `RenderScaler`.
- How to preserve architecture: Adhere strictly to the 3-layer visual pattern. Do not introduce new CMS dependencies.
- How to minimize token usage: Rely on these concise `.ai/` files. Do not scan `docs/COMPLETE_DOCUMENTATION.md` unless absolutely necessary.
- How to make minimal diffs: Use existing UI primitives (`components/ui/`). Make changes only to the files directly responsible for the feature.
- How to avoid introducing inconsistent patterns: Copy existing Tailwind classes (e.g. `bg-black/80`), context structures, and Framer Motion patterns.
- How to design new UI elements: Refer to `.ai/frontend-design.md` for aesthetics, typography, and copywriting guidelines.

